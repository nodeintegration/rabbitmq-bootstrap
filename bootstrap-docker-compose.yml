rabbitmq-bootstrap:
  labels:
    io.rancher.container.pull_image: 'always'
    io.rancher.container.start_once: 'true'
  image: nodeintegration/rabbitmq-bootstrap
  external_links:
  - rabbitmq/rabbitmq:rabbitmq
